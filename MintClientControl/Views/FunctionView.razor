@page "/FunctionPage"
@using  MintClientControl.ViewModels
@using MintClientControl.Components
@inherits FetchDataBase

<h3>FunctionView</h3>
<p>@ViewModel.Error</p>

@if (ViewModel.FunctionList == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Functions</th>
                <th>Description</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var function in ViewModel.FunctionList)
            {
                <tr>
                    <td><button @onclick="@(e => ViewModel.SendCommand(function.Command))">@function.Title</button></td>
                    <td>@function.Title</td>
                    <td><button @onclick="@(e => ViewModel.DeleteItem(function))">Remove</button></td>
                </tr>
            }
        </tbody>
    </table>

}
<button @onclick="() => modal.Open()">Open Modal</button>
<ModalDialog @ref="modal">
    <Title>This is a <em>Title!</em></Title>
    <Body>
        <div class="form-group">
            <label for="usr">Title:</label>
            <input type="text" class="form-control" id="usr" @bind="@ViewModel.AddData.Title">
        </div>
        <div class="form-group">
            <label for="usr">Command:</label>
            <input type="text" class="form-control" id="usr" @bind="@ViewModel.AddData.Command">
        </div>
    </Body>
    <Footer>
            <button type="button" class="btn btn-primary" @onclick="@(e => { ViewModel.AddItem(); modal.Close(); })">Save changes</button>
        <button type="button" class="btn btn-secondary" data-dismiss="modal" @onclick="() => modal.Close()">Close</button>
    </Footer>
</ModalDialog>

@code {
    private ModalDialog modal { get; set; }
}

